/**
 * @ignore
 */ /** */
import { Aqi } from '../types';
import { Pollutant } from './pollutant';
/**
 * Round a number to closest 10^-{decimal}
 *
 * @param n - The float number to round
 * @param decimal - The number of decimals after the .
 */
export declare function round(n: number, decimals?: number): number;
declare type Piecewise = [number, number][];
/**
 * Piecewise breakpoints that define an AQI
 */
export declare type Breakpoints = Record<'aqi', Piecewise> & Partial<Record<Pollutant, Piecewise>>;
/**
 * Create an AQI from its breakpoints
 *
 * @param aqiCode - The code of the AQI
 * @param breakpoints - The breakpoints defining the AQI
 */
export declare function createAqiFromBreakpoints(aqiCode: string, breakpoints: Breakpoints): Omit<Aqi, 'displayName'>;
export {};
