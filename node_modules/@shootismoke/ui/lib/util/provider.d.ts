import { Options } from 'async-retry';
import type { Api } from './api';
/**
 * Fetch data from correct provider, based on stationId.
 *
 * @param stationId - The stationId of the station
 */
export declare function fetchStationId(stationId: string, options?: Options): Promise<Api>;
