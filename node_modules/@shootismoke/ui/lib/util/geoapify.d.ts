import { LatLng } from '@shootismoke/dataproviders';
export interface GeoapifyRes {
    city?: string;
    country?: string;
    formatted: string;
    lat: number;
    lon: number;
    state?: string;
}
export declare function geoapify(search: string, apiKey: string, gps?: LatLng): Promise<GeoapifyRes[]>;
