"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
exports.__esModule = true;
var pollutant_1 = require("./pollutant");
var testutil_1 = require("./testutil");
describe('getAQI', function () {
    var testCases = [
        __assign(__assign({}, testutil_1.testCases[0]), { expected: 166 }),
        __assign(__assign({}, testutil_1.testCases[1]), { expected: 26 }),
    ];
    it('should work with testCases', function () {
        testCases.forEach(function (tc) {
            var aqi = (0, pollutant_1.getAQI)(tc.results);
            expect(aqi).toBe(tc.expected);
        });
    });
});
describe('getPollutantData', function () {
    it('should work with co', function () {
        var pollutantData = (0, pollutant_1.getPollutantData)('co');
        expect(pollutantData).toEqual({
            effects: 'Carbon monoxide reduces the oxygen-carrying capacity of the blood by combining with haemoglobin, thus deprives tissues of O2.',
            name: 'Carbon monoxide'
        });
    });
});
