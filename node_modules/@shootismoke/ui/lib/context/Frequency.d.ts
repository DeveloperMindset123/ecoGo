import React from 'react';
import { Frequency } from '../util/types';
interface Context {
    frequency: Frequency;
    setFrequency: (newFrequency: Frequency) => void;
}
export declare const FrequencyContext: React.Context<Context>;
export declare function FrequencyContextProvider({ children, }: {
    children: JSX.Element;
}): React.ReactElement;
export {};
