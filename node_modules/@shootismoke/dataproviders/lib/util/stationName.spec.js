"use strict";
exports.__esModule = true;
var stationName_1 = require("./stationName");
describe('stationName', function () {
    it('should take attribution if it exists', function () {
        expect((0, stationName_1.stationName)({ attribution: [{ name: 'foo' }] })).toBe('foo');
    });
    it('should take location name', function () {
        expect((0, stationName_1.stationName)({ location: 'foo' })).toBe('Station foo');
    });
});
