"use strict";
exports.__esModule = true;
var countryCode_1 = require("./countryCode");
describe('getCountryCode', function () {
    it('should match exact country', function () {
        expect((0, countryCode_1.getCountryCode)('United States')).toEqual('US');
    });
    it('should match lower case', function () {
        expect((0, countryCode_1.getCountryCode)('united states')).toEqual('US');
    });
    it('should match multiple spacing', function () {
        expect((0, countryCode_1.getCountryCode)('united  states')).toEqual('US');
    });
    it('should match dashed', function () {
        expect((0, countryCode_1.getCountryCode)('saudi-arabia')).toEqual('SA');
    });
    it('should match included string', function () {
        expect((0, countryCode_1.getCountryCode)('United States Of America')).toEqual('US');
    });
});
