"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
exports.__esModule = true;
exports.normalize = void 0;
var util_1 = require("../../util");
/**
 * Normalize aqicn byGps data. Throws an error if the data cannot be normalized.
 *
 * @param data - The data to normalize
 */
function normalize(data) {
    var results = data.results;
    if (!results.length) {
        throw (0, util_1.providerError)('openaq', 'Cannot normalize, got 0 result');
    }
    return results.map(function (result) { return (__assign(__assign({}, result), { location: "openaq|".concat(result.location) })); });
}
exports.normalize = normalize;
